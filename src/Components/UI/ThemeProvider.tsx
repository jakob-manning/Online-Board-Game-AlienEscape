import { createMuiTheme } from '@material-ui/core/styles';

export const materialTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#ba68c8',
        },
        secondary: {
            main: '#ff4081',
        },
    },
});